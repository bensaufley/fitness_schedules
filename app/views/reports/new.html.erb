<%= provide(:title, 'New Report') %>

Please enter a trainer's email address to pull up a report of all their sessions:

<%= form_for(:report, action: :create, url: '/reports') do |f| %>
  
  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.submit "Generate Report" %>

<% end %>